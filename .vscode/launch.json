{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Run ./bilang",
            "program": "${workspaceFolder}/src.bin",
            "args": [],
            "cwd": "${workspaceFolder}"
        },
    ]
}